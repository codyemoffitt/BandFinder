extends layout

block content

  if (!user)
    a(href="/auth/google") Please login with Google
    br
  if (user)
    p Welcome #{user.profile.name.givenName}
    a(href="/auth/google/logout") Logout
    br


